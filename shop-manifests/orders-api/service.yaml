apiVersion: v1
kind: Service

metadata:
  name: orders-api
  namespace: shop

  labels:
    app: orders-api


spec:

  selector:
    app: orders-api


  ports:

  - name: http
    port: 80
    targetPort: 3000


  type: ClusterIP
